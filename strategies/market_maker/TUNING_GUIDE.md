# 价差分析与参数调优指南

## 使用说明

### 1. 启动价差分析工具

在新终端运行：

```bash


# 运行 60 分钟（默认）
uv run python strategies/market_maker/analyze_spread.py

# 或指定时长（如 10 分钟快速测试）
uv run python strategies/market_maker/analyze_spread.py 10
```

### 2. 观察对比结果

工具会显示实时对比表格：

```
┌─────────┬───────────┬───────────┬─────────────┬──────────┬──────────┬────────────┬──────────────┐
│ Symbol  │ MEXC Bid  │ MEXC Ask  │ MEXC Spread │ Our Bid  │ Our Ask  │ Our Spread │ Competitive? │
├─────────┼───────────┼───────────┼─────────────┼──────────┼──────────┼────────────┼──────────────┤
│BTC/USDT │ 89450.00  │ 89455.00  │   0.006%    │ 89417.30 │ 89506.77 │   0.100%   │ ❌ 太宽      │
└─────────┴───────────┴───────────┴─────────────┴──────────┴──────────┴────────────┴──────────────┘
```

### 3. 查看统计与建议

运行结束后会显示：

```
📊 统计分析结果

BTC/USDT:
  MEXC 平均价差: 0.008%
  当前设置 (sigma=0.002): 0.100%
  建议 sigma: 0.00013
  预期价差: 0.006%
  💡 可以缩窄价差以提高竞争力
```

---

## 参数调整方法

### 步骤 1：根据分析结果调整

编辑 `strategies/market_maker/core/algo.py`:

```python
# 找到第 58 行附近
# 将 sigma 从 0.002 改为建议值

# 示例：如果建议 sigma = 0.00013
spread_pct = sigma * 50  # 当前
# 改为
spread_pct = 0.00013 * 50  # 约 0.0065%
```

### 步骤 2：重启 Dashboard 验证

```bash
# Ctrl+C 停止当前 Dashboard
# 重新启动
uv run python -m strategies.market_maker.dashboard --auto-discover --limit 3
```

### 步骤 3：再次对比

运行分析工具 5-10 分钟，确认新价差：

```bash
uv run python strategies/market_maker/analyze_spread.py 10
```

---

## 调优目标

### 理想状态

- **我们的价差** = MEXC 平均价差 × 0.8 (略窄 20%)
- **竞争力标记**: ✅ 更窄

### 示例场景

**场景 1：MEXC 价差很窄（0.01%）**

- 建议 sigma: 0.00016 (价差 0.008%)
- 策略：紧跟市场，小幅盈利

**场景 2：MEXC 价差适中（0.05%）**

- 建议 sigma: 0.0008 (价差 0.04%)
- 策略：保持竞争力

**场景 3：MEXC 价差很宽（0.2%）**

- 当前 sigma: 0.002 (价差 0.1%) ✅
- 策略：已经很有竞争力，保持当前设置

---

## 注意事项

⚠️ **价差不是越窄越好**：

- 太窄 (< 0.02%) → 容易被"夹击"（买卖单都成交但没利润）
- 太宽 (> 0.5%) → 缺乏竞争力（订单不会成交）

✅ **最佳策略**：

- 在保证盈利空间的前提下，比 MEXC 窄 10-20%

---

## 快速参考

| MEXC 平均价差 | 建议 sigma | 预期我们的价差 |
| ------------- | ---------- | -------------- |
| 0.01%         | 0.00016    | 0.008%         |
| 0.05%         | 0.0008     | 0.04%          |
| 0.10%         | 0.0016     | 0.08%          |
| 0.20%         | 0.0032     | 0.16%          |

当前默认：sigma = 0.002 (价差 0.1%)
